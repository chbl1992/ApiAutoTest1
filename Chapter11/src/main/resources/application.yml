server:
  port: 9999

logging:
  path: logs
  file: mylog.log

spring:
  application:
    name: myTest
  datasource:
    driver-class-name: com.mysql.jdbc.Driver
    url: jdbc:mysql://localhost:3306/muke
    username: root
    password: 123456

mybatis:
  type-aliases-package: com.muke.model
  mapper-locations:
    - mapper/*
